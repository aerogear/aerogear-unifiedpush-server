<!--Template to create a new push application-->
<div>
  {{#if variantID}}
    Edit a Variant
  {{else}}
    Add Variant
  {{/if}}
</div>
<div>
    <div>
      <form method="POST" {{action "add" controller target="controllers.variantsIndex" on="submit" }} >
      <fieldset>
        <p>
          <label>Name</label>
          {{input type="text" name="name" value=variantName}}
        </p>
        <p>
          <label>Description</label>
          {{textarea name="description" value=variantDescription}}
        </p>
        {{ partial "variants/addVariantsNetwork" }}
        <input type="reset" class="primary" value="Cancel" {{ action "cancel" controller target="controllers.variantsIndex"}}>
        <input type="submit" class="primary" value="Create">
      </fieldset>
    </form>
    </div>
</div>
