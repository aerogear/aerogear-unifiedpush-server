{{headAppTitle name}}
<aside  {{bind-attr class="showReset:topcoat-overlay:hidden"}}>
        <h1>Renew Master Secret</h1>
        <p>This means that you can no longer use this Master Secret to send push notifications to installations. A new Master Secret will be created for you. You may want to do this for security reasons.</p>
        <p>Are you sure you want to renew the Master Secret?</p>
        <div class="topcoat-overlay-buttons">
            <button role="button" class="topcoat-button--cta" {{action 'resetMaster' this}} {{bindAttr disabled="controllers.application.isProcessing"}}>Renew Master Secret</button>
            <button role="button" class="topcoat-button" {{action 'toggleResetOverlay'}}>Cancel</button>
        </div>
</aside>
<aside  {{bind-attr class="showSend:topcoat-overlay:hidden"}}>
        <h1>Send test message</h1>
     <div>
       {{textarea type="text" value=testMessage}}
     </div>
        <div class="topcoat-overlay-buttons">
            <button role="button" class="topcoat-button--cta" {{action 'sendTestMessage' testMessage}} {{bindAttr disabled="controllers.application.isProcessing"}}>Send Test Message</button>
            <button role="button" class="topcoat-button" {{action 'toggleSendOverlay'}}>Cancel</button>
        </div>
</aside>
<aside  {{bind-attr class="showVariantDelete:topcoat-overlay:hidden"}} id="deleteVariantOverlay">
        <h1>Remove <strong>" {{ content.variantToDelete.name }} "</strong></h1>
        <p>Are you ABSOLUTELY sure ?</p>
        <p>Please type in the name of the Variant to confirm.</p>
        <p>{{input type="text" placeholder="Variant name" valueBinding="confirmVariantName" classNames="name topcoat-text-input" }}</p>
        <div class="topcoat-overlay-buttons">
            <button role="button" id="deleteVariantButton" class="topcoat-button--cta" {{action 'remove' }} {{bindAttr disabled="disableVariantDeleteButton"}}>Remove</button>
            <button role="button" class="topcoat-button" {{action 'toggleVariantDeleteOverlay'}}>Cancel</button>
        </div>
</aside>
<div>{{#linkTo "mobileApps"}}&larr; Back to Applications{{/linkTo}}</div>

<section>
        <h2>Application:
            <strong>&ldquo;{{ name }}&rdquo;</strong>
        </h2>
        <span>{{ description }}</span>
</section>

<section>
        <h3>Sending push notifications</h3>
        <span>
            Use this <i>Application ID</i> and <i>Master Secret</i> combination to send push notifications to registered installations.
        </span>
        <div>
            Application ID:   {{input type="text" disabled="true" class="rcue-code" valueBinding="pushApplicationID"}}<br>
            Master Secret:    {{input type="text" class="rcue-code" valueBinding="masterSecret" disabled="true"}}  <a href="#" {{ action "toggleResetOverlay" this}}>Renew...</a>
        </div>
        <div>
         {{#linkTo "variants.compose" this}}Compose Message...{{/linkTo}}

</section>

<section>
    <h3>Variants</h3>
    {{#if isLoaded}}
        {{ partial "variants/variantsDetail" }}
    {{else}}
        loading....
    {{/if}}
</section>

