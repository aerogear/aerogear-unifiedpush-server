language: java

jdk:
  - oraclejdk7
  - openjdk6


before_install:
  - git clone git://github.com/matzew/keycloak
  - cd keycloak
  - mvn install -DskipTests -N --quiet
  - cd ..

notifications:
  irc: "irc.freenode.org#aerogear"

branches:
  only:
    - master  

script: "mvn verify"
