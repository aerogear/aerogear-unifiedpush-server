language: java

jdk:
  - oraclejdk7
  - openjdk6


before_install:
  - git clone git://github.com/keycloak/keycloak
  - cd keycloak
  - mvn install -DskipTests
  - cd ..

notifications:
  irc: "irc.freenode.org#aerogear"

branches:
  only:
    - master  

script: "mvn verify"
